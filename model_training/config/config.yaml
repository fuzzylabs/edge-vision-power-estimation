data:
  data_dir: "training_data"
  test_models: ["lenet", "vgg16"]

model:
  cross_validation: 10

mlflow:
  enable_tracking: True
  mlflow_experiment_name: "pytorch-exp"
  dagshub_repo_owner: "fuzzylabs"
  dagshub_repo_name: "edge-vision-power-estimation"
